class Pacer(): 
    def __init__(self, num_instances, num_iters): 
        # num_iters is epochs * num_instances/batch_size 

    def get_fraction(self) -> int: 
        """
        Return what fraction of the dataset next minibatch should be
        sampled from.

        Object keeps track of  how many iterations are already completed. 
        """


# Implement pacing functions here
